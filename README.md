## Grocery Shopping Schedule Planner with Nearby Store Discovery, Map View, Exports, and Bilingual UI

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
